model:
  hidden_dim: 64
  emb_dim: 32
  dropout: 0.0

train:
  lr: 0.001
  epochs: 20
  seed: 42
  device: cpu

paths:
  train_data: artifacts/minimal_gnn/train.pkl
  val_data: artifacts/minimal_gnn/val.pkl
  checkpoint: artifacts/minimal_gnn/minimal_gnn.pt
